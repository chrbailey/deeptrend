{
  "name": "deeptrend",
  "version": "0.2.0",
  "description": "Trend intelligence pipeline — scrapes Google Trends, Reddit, arXiv, Moltbook, and X/Twitter, analyzes via claude -p with velocity scoring",
  "type": "module",
  "scripts": {
    "start": "tsx src/cli.ts",
    "scrape": "tsx src/cli.ts --scrape-only",
    "analyze": "tsx src/cli.ts --analyze",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "@supabase/supabase-js": "^2.95.3",
    "commander": "^14.0.3",
    "dotenv": "^17.3.1",
    "fast-xml-parser": "^5.3.6",
    "playwright": "^1.58.2"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
